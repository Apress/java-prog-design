Chapter 5 ThreadFactory Example

The class PriorityThreadFactory demonstrates how to use the
ThreadFactory interface. Each PriorityThreadFactory object
has an associated priority. Each Thread object it creates
will have that priority.
