Count Demo, Version 3.

Add a new view/controller to version 2, 
which watches the current count.

The count view and its controller are rewritten
to conform to the MVC pattern. In particular, the
controller is now a model observer. 

Both views are displayed in a single window.
